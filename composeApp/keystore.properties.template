# Copy to composeApp/keystore.properties and fill in. Never commit the real file.
# storeFile is resolved relative to the repository root.
storeFile=ts_iptv_android_key.jks
storePassword=
keyAlias=ts_iptv
keyPassword=
